==================================================
PROJECT: Signal Scorer Pro
LAST UPDATED: 2026-05-20 13:50:00 IST
==================================================

CURRENT TECH STACK
--------------------------------------------------
Frontend:
- React 18 (UI Library)
- Vite 5 (Build Tool & Dev Server)
- TailwindCSS 3 (Styling & Dark Terminal Theme)
- Zustand (Global State Management)
- Lucide React (Icons)

Backend:
- Node.js 22 (Production host node runtime)
- Express 4 (REST API Server)
- Socket.io 4 (Real-time WebSocket communication)
- Node-Cron (Background scheduling)
- ws (Native WebSocket client for Binance price stream)

Database:
- better-sqlite3 (Ultra-fast, zero-config embedded SQL database)
- Tuned with: WAL mode, NORMAL sync, 64MB cache, MEMORY temp store

Integrations:
- Binance USDT Futures API (Public REST + WebSocket streams)
- Telegram Bot API (telegraf library)
- Google Gemini AI (gemini-1.5-flash for signal validation)

Fault Tolerance:
- CircuitBreaker pattern on Binance & Gemini API calls
- Exponential backoff reconnection on WebSocket drops
- Concurrency limiter (pLimit) for kline batch fetches

==================================================
SYSTEM ARCHITECTURE
--------------------------------------------------

[ Binance WS ] ===(wss://fstream.binance.com/market/ws)===> [ priceStream.js ] (0 cost, 1s tick)
                                                               |
                                                               v
[ Binance REST ] <---(HTTP GET / pLimit)--- [ Node/Express cronJobs.js ] <---(Socket.io)---> [ React Frontend ]
                                                               |
                                                               v
                                                      [ SQLite (WAL Mode) ]
                                                               |
                                                               v
                                                    [ Telegram & Gemini AI ]

Architecture Flow:
1. priceStream.js maintains a single persistent WebSocket to Binance Futures market stream, maintaining a sub-second in-memory live price cache of all USDT perpetuals for zero API weight cost.
2. cronJobs.js wakes up every 5 minutes and calls Binance REST via binanceBreaker (Circuit Breaker) for the Top 150 pairs.
3. It fetches 15m and 1h candlestick data in batches using a concurrency limiter (pLimit) to prevent rate limit limits.
4. The data is fed into SignalScoringEngine.js.
5. The Engine scores each pair using statistically decorrelated weights, volume Z-scores, and ATR-based dynamic TP/SL levels. Ranging markets are skipped entirely via the ADX regime filter.
6. Signals matching settings.minScore are instantly persisted to SQLite database (optimized with WAL mode + Normal sync to prevent write contention) and high-confidence signals are validated by Gemini AI via geminiBreaker.
7. The Backend broadcasts the new signals via Socket.io to the Frontend.
8. The Backend sends Telegram alerts for signals with Score >= 75.
9. Every 5 seconds, cronJobs.js evaluates active database signals against the local WebSocket price cache (zero API calls).
10. If TP/SL is hit, the database marks the signal as WIN/LOSS and updates the Win Rate on the frontend.

==================================================
API ENDPOINTS
--------------------------------------------------
1. GET /api/meta
   - Returns basic server health and version info.
2. POST /api/scan
   - Manually triggers a fresh Binance market scan.
3. GET /api/signals
   - Returns the latest signals from the cache.
4. GET /api/signals/history
   - Returns all closed historical signals.
5. GET /api/system-notes
   - Returns project documentation contents.
6. GET /api/settings & POST /api/settings
   - Fetches and updates dynamic AI engine weights.
7. GET /api/health
   - Verifies Binance API, SQLite, Telegram, and Gemini status, including Circuit Breaker states.
8. POST /api/telegram/broadcast
   - Broadcasts a custom HTML announcement to Telegram channels.
9. GET /api/logs & DELETE /api/logs
   - Fetches and clears persistent SQLite system exceptions and logs.
10. POST /api/logs/test
    - Simulates a diagnostics system exception for testing.
11. POST /api/webhook
    - (Production only) Receives incoming messages from Telegram.

==================================================
FEATURE: System Error Logs
--------------------------------------------------
Backend:
- SQLite table 'error_logs' tracking exceptions.
- Custom 'logError' helper integrated across Binance, Gemini AI, Scanner, and PriceTracker.
- Exposes rich API endpoints for frontend log retrieval.

Frontend:
- Dedicated 'Error Logs' Tab on Dashboard.
- Displays errors with contextual tags, timestamps, and expandable stack traces.
- Features 'Simulate Error' to trigger instant diagnostic logging and verification.
- Features 'Clear Logs' for single-click maintenance.

==================================================
DATABASE TABLES (SQLite)
--------------------------------------------------
Table: signals
Stores every high-confidence trading signal to track its performance.

Columns:
- id (Primary Key, format: SYMBOL_TIMESTAMP)
- symbol (e.g. BTCUSDT)
- direction (LONG or SHORT)
- score (0 to 100)
- confidence (LOW, MEDIUM, HIGH, VERY_HIGH)
- entry (Price at time of signal)
- tp1, tp2, stopLoss (Calculated price targets)
- status (ACTIVE, WIN_TP1, WIN_TP2, LOSS_SL, EXPIRED)
- createdAt, closedAt (Unix timestamps)
- maxProfitPct (Maximum profit percentage achieved)
- reasons, subScores (JSON strings of engine data)

Table: settings
Stores dynamic AI Engine weights and configuration limits.
Columns:
- key (Primary Key, e.g., "emaAlignment")
- value (e.g., "0.25")

==================================================
DEPLOYMENT PROCESS
--------------------------------------------------
1. Clone repository to VPS.
2. Run `npm install` to install dependencies (compiles better-sqlite3).
3. Copy `.env.example` to `.env` and fill in keys.
4. Run `npm run build` to compile the Vite frontend into the `/dist` folder.
5. Use PM2 to run the server in production: `NODE_ENV=production pm2 start server/index.js`
   (In production, Express serves the API on port 8080 AND serves the frontend UI from `/dist` on the same port).
6. Configure Nginx reverse proxy to point to port 8080 and attach SSL.

==================================================
ENVIRONMENT VARIABLES & PORTS
--------------------------------------------------
Ports (Dev Mode):
- Backend API: 8080
- Frontend Vite UI: 8081

Environment Variables (.env):
- NODE_ENV (development / production)
- PORT (Backend port, default 8080)
- BINANCE_API_KEY / BINANCE_SECRET_KEY (For future authenticated endpoints)
- TELEGRAM_BOT_TOKEN (From @BotFather)
- TELEGRAM_CHAT_IDS (Channel IDs to broadcast to)
- GEMINI_API_KEY (For AI validation)
- VITE_SOCKET_URL (http://localhost:8080 in dev, empty in prod)

==================================================
KNOWN ISSUES & PRODUCTION WORKAROUNDS
--------------------------------------------------
- Binance IP Rate Limits: If the scan frequency is too high or the batch size too large, Binance returns HTTP 418 (IP Banned). Mitigated by 5-min intervals and 150-pair limits.
- Hostinger Node Version ABI Mismatch: Node major version updates can mismatch better-sqlite3's C++ bindings (e.g., compiled on NODE_MODULE_VERSION 115 but run on 137). Resolved by performing a complete Node rebuild/reinstall (`rm -rf node_modules package-lock.json && npm install`) on the production VPS.
- Binance Legacy WS Stream Drop: Connecting directly to legacy stream paths (e.g. `fstream.binance.com/ws`) accepts connection handshakes but silently drops all message events. Bypassed by updating to the newly required routed market path: `wss://fstream.binance.com/market/ws/!miniTicker@arr`.

==================================================
COMPLETED FEATURES
--------------------------------------------------

FEATURE: AI Signal Scoring Engine
Backend:
- `SignalScoringEngine.js` evaluates 5 weighted factors.
- Calculates dynamic TP1/TP2/SL based on ATR (Average True Range).
Flow: Candlesticks -> Engine -> Numeric Score (0-100) -> Signal Object

FEATURE: Real-Time UI Dashboard
Frontend:
- Built with React and Tailwind CSS.
- Uses Zustand to manage global state and WebSocket connections.
- Displays glowing animated rings, scrolling tickers, and glassmorphic cards.
Flow: Backend -> Socket.io -> Zustand Store -> React Components

FEATURE: Telegram Broadcasting
Backend:
- Uses `telegraf` library.
- Formats signals into an easy-to-read HTML block with emojis.
- Deduplicates alerts so the same pair isn't spammed within 30 minutes.
Flow: Signal Generated -> Check Score >= 75 -> Format HTML -> Send to Telegram API

FEATURE: SQLite Persistence & Win-Rate Tracking
Backend:
- `better-sqlite3` records all signals into `signal-scorer.db`.
- 10-second price loop evaluates active signals against live prices.
- Automatically closes signals as WIN or LOSS when price crosses TP/SL targets.
Flow: Live Price Tick -> SQLite Query -> Compare Entry/TP/SL -> Update DB Status -> Emit updated Win Rate to Frontend.

FEATURE: Trade History Dashboard
Frontend:
- `HistoryTable.jsx` displays closed trades.
- `Dashboard.jsx` handles tab toggling between Active and History.
- Fetches from `GET /api/signals/history`.
Flow: User clicks Tab -> Zustand `fetchHistory` -> API -> SQLite Query -> React UI

FEATURE: System Notes UI
Frontend/Backend:
- Backend exposes `GET /api/system-notes` which reads `project_system_notes.txt`.
- Frontend `Dashboard.jsx` has a "System Notes" tab to render the content in real-time.
Flow: User clicks Tab -> Zustand `fetchSystemNotes` -> API (`fs.readFileSync`) -> React UI `<pre>` block

FEATURE: System Health & AI Parameter Engine
Backend:
- `health.js` route dynamically tests external API dependencies.
- `settings.js` handles persistent SQLite parameters for the scoring engine.
Frontend:
- System Health Tab visually displays ping/status for Binance, SQLite, Telegram.
- Settings Tab provides sliders to hot-swap engine calculation weights.

FEATURE: Automated Expired Signals Cleanup
Backend:
- `deleteExpiredSignals` automatically purges database EXPIRED signals older than 7 days on startup to maintain peak performance and save disk space.

==================================================
SYSTEM ARCHITECTURE & PRODUCTION STABILITY PATCHES (MAY 19, 2026)
--------------------------------------------------
1. PRODUCTION WS FIX: Legacy WebSocket connection string `wss://fstream.binance.com/ws/!miniTicker@arr` successfully connected but returned zero data due to Binance stream deprecation. Upgraded connection endpoint to the modern routed URL: `wss://fstream.binance.com/market/ws/!miniTicker@arr`.
2. DYNAMIC 24H CHANGE CALCULATION: Since the `!miniTicker@arr` stream does not natively expose a pre-calculated `change24h` / `P` property (leading to stuck `+0.00%` on frontend cards), implemented a dynamic, real-time formula mapping in `priceStream.js` using open price (`t.o`) and close price (`t.c`) relative percent difference: `((close - open) / open) * 100`.
3. PRISTINE PURGE ALL DATA: Wipes database signals, errors, and resets 12-hour scanner deduplication cache. Enhanced the Zustand frontend client socket handler with nullish coalescing to prevent fallback errors when `totalPairs` is `0`, and set `scannedAt: null` on purge so that the dashboard pristine state reads `Last scan: Never` and `0 pairs` beautifully.
4. HOSTINGER SQLITE DRIVER REBUILD: Cleansed Node package dependencies on Hostinger VPS and rebuilt standard native compiler paths to eliminate native Better-SQLite3 compilation mismatches.
5. AUTO-CLEANUP ON STARTUP: Integrated startup SQLite cleanup targeting EXPIRED signals older than 7 days, preventing database bloat and keeping indexes hot.
6. DAILY CLEANUP CRON SCHEDULE: Added a daily cron job scheduled at 03:00 AM in `cronJobs.js` to execute `deleteExpiredSignals(7)` continuously.
7. DUPLICATE ACTIVE SIGNAL GUARD: Modified `insertSignals` in `database.js` to dynamically reject insertions for symbols that already have an active trade open, preventing whipsaw and spam alerts during high volatility.
8. EMPIRICAL WIN-RATE LOOKUP: Integrated real-time 30-day historical win-rate calculation inside `insertSignals` (`getSymbolWinRate`). Automatically scans preceding resolved trade statistics for a specific symbol & direction to attach empirical win rates and sample sizes to incoming active signals for high-confidence trading.
9. 4H MULTI-TIMEFRAME TREND CONFLUENCE: Integrated a high-order 4h trend filter in `SignalScoringEngine.js` using EMA(9)/EMA(21) alignments and EMA(200) prices. Dynamically filters out short-term counter-trend signals to eliminate whipsaws and elevate edge.
10. PM2 PRODUCTION DAEMON ECOSYSTEM: Added custom `ecosystem.config.js` to manage process lifecycles, memory boundaries (512MB), graceful shutdown (5s), structured system logging `/var/log/signal-scorer/`, systemd boot survivability, and configured public listener to use PORT 9000 to match VPS Nginx reverse proxy firewall rules.
11. R:R NOTATION ALIGNMENT: Aligned all occurrences of the Risk-to-Reward ratio to use standard `R:R` notation instead of `R/R` uniformly across the Telegram alert templates, Gemini AI validator prompts, and frontend Signal Cards for premium branding.
12. ROBUST ATR SCORING MULTIPLIERS: Optimized default ATR trade parameters (Stop Loss widened to 2.0x, TP1 expanded to 2.5x, TP2 expanded to 4.5x) inside `SignalScoringEngine.js` to mitigate premature noise stop-outs and accommodate market swing tolerances.
13. REMOVAL OF 12-HOUR COOL-OFF LIMIT: Completely removed the 12-hour scanner cool-off limit from `cronJobs.js` to allow highly conformed concurrent signals to pop up repeatedly whenever conditions and parameter rules align perfectly.
14. TWO-STAGE ACTIVE SIGNAL STATE MACHINE: Migrated database schema to support `tp1Hit` column. Refactored `updateSignalStatus` in `database.js` and `processPriceUpdate` in `cronJobs.js` so that hitting TP1 transitions the active signal stopLoss to the entry level (breakeven) to lock in gains and keeps it active on the dashboard. Prevents multiple re-triggers, and ensures returning to breakeven records maxProfitPct as 0 instead of negative. Replaced `getHistoricalStats` to calculate per-target accuracy ratios and overall trading Expectancy (R).
15. PREMIUM DASHBOARD METRICS INTERFACE: Replaced the standard 4-stat stats-row with an advanced 6-card dashboard surfacing Total Signals, Long/Short split, TP1 Touch Rate, TP2 Hit Rate, Stop Losses count, and R Expectancy. Augmented individual `SignalCard` and `SignalDetail` modals with dynamic multi-colored indicator badges (4H Bias, Empirical 30d Win Rate, Active TP1/Breakeven flags) and real-time percentage distance distance metrics from entry.
16. DYNAMIC DASHBOARD PARAMETERS & COOLDOWNS: Integrated dynamic controls in the settings tab for the 30-minute signal cooldown period, and ATR multipliers (Stop Loss, Take Profit 1, Take Profit 2). Programmed automatic seeding on startup, dynamic lookup within the `insertSignals` block, and active parameter override injection in the `SignalScoringEngine` constructor.
17. COMPREHENSIVE ERROR COVERAGE LOGS: Extended the global `logError()` pipeline directly into the Binance API retry mechanisms and symbol klines fetching catch boundaries. Every transient warning, rate limit warning, or symbol API timeout is now securely cataloged into the SQLite database and instantly populated on the dashboard.
18. MANUAL SCAN ROUTE CORRECTION: Resolved a critical mismatch where the frontend's manual rescan action was targeting `/api/signals/rescan` (returning a 404), rendering manual dashboard rescans silent. Re-routed to the correct active endpoint `/api/scan` to ensure seamless, real-time manual triggers.
19. REAL-TIME LIVE SCANNER TERMINAL: Developed an in-memory sliding logs buffer (`scannerLogBuffer`) to record scanner steps and engine metrics. Mounted a dedicated `GET /api/logs/scanner` REST endpoint and configured real-time WebSocket broadcasting over `scan:log`. Designed and integrated a premium green-on-black scrolling terminal console tab on the dashboard, providing full visibility into technical scans.
20. DYNAMIC MINSCORE CACHE RESOLUTION: Fixed a backend bug where getSignals() fell back to a hardcoded default minimum score of 60 when triggered with no parameters (such as in WebSocket broadcasts and initial route mounting). Refactored cache.js to dynamically resolve the minimum score against active SQLite user settings (e.g. 5), allowing lower scoring signals to populate correctly without being discarded.
21. WEBSOCKET TICKER OBJECT EXTRACTION: Fixed a critical price tracking bug where the centralized WebSocket mini-ticker stream stored objects `{ price: closePrice, change24h: changePercent }` inside the price map cache, but processPriceUpdate() and WebSocket broadcasts emitted the object directly instead of extracting the primitive numeric price. This caused arithmetic comparisons against entries/levels to evaluate to NaN (preventing TP/SL active resolutions) and resulted in a $[object Object] visual render bug on the frontend ticker tape and signal cards. Refactored server/cronJobs.js to correctly extract and map the numeric price properties.
22. ADVANCED TRADING PERFORMANCE METRICS: Redesigned the top statistics display of the dashboard to align with institutional-grade risk management. Separated TP1 and TP2 tracking rates: mapped TP1 Hit Rate to all signals touching TP1 (tp1TouchRate) and TP2 Hit Rate to full target completions (tp2HitRate). Upgraded Overall Accuracy to a Weighted Win Rate where TP2 hits receive a 1.0 weight, TP1-only hits receive a 0.5 weight, and immediate Stop Losses receive 0.0 weight. Calculated and introduced mathematical Expectancy (E) directly from historical signal percentage returns, enabling instant evaluation of trading system profitability. Refactored server/services/database.js, StatsBar.jsx, and signalStore.js.
23. PAIR SYMBOL REAL-TIME FILTER: Added a case-insensitive search bar directly within the active signals filter bar. Configured the Zustand global store (`src/store/signalStore.js`) to support dynamic `search` filters and optimized `applyFilters()` to process active list text queries instantaneously on the client-side. Styled the new element with premium placeholder guides and built-in search clearing actions using Lucide icons inside `src/components/FilterBar.jsx`.
24. SIGNAL STARTUP EXPIRY CLEANUP: Integrated a deleteExpiredSignals(7) execution at server start within initCronJobs() to purge stale historical data before initiating the first scan.
25. GRACEFUL WEBSOCKET SHUTDOWN: Added SIGTERM and SIGINT process event listeners to cleanly terminate the persistent Binance WebSocket connection on PM2 stop/restart.
26. RATE-LIMIT HEADER TRACKING: Configured Binance REST clients in lib/binance.js to read the x-mbx-used-weight-1m response headers as the authoritative source of truth for rate-limit weight tracking.
27. COMPOSITE DATABASE INDEX: Created a composite index (symbol, status) on the signals table to optimize and speed up getActiveSignals() queries.
28. HISTOGRAM STATS ENDPOINT: Implemented the GET /api/signals/stats endpoint to return bucketed 10-point score distribution metrics with direction and confidence segmentations.
29. WEBSOCKET HEALTH CHECK STATUS: Added a priceStream field reporting the WebSocket connectivity status (CONNECTED, CONNECTING, DISCONNECTED) inside the /api/health endpoint.
30. TELEGRAM ALERT PERCENTAGE MARKERS: Upgraded Telegram signal notifications to dynamically calculate and append percentage gains/losses for Entry, Take Profit 1, Take Profit 2, and Stop Loss targets.
31. SCAN DURATION IN CACHE & HEADER: Exposed scanDurationMs on signal metadata and mapped it through to the Zustand store, enabling the header to display "took Xs" metrics next to the scan time.
32. UPGRADED FRONTEND SIGNAL BADGES: Redesigned and integrated premium gold "TP1 ✓" badges on Signal Cards to clearly indicate when partial exits and breakeven SL transitions have occurred.
33. TOTAL ACTIVE SIGNALS COMPONENT: Integrated a dedicated "Active Signals" card inside the dashboard stats grid showcasing currently open/unresolved signals count, and added a real-time glowing pill count badge next to the tab header.
34. PERSISTENT RISK-REWARD CALCULATION CORRECTION: Resolved a database schema mismatch where the riskReward property was not persisted inside the signals SQLite table. Computed riskReward dynamically on the fly during active DB signals retrieval in cache.js and implemented a zero-latency client-side calculation fallback inside SignalCard.jsx to eliminate visual "x" (undefined) renders.
35. INSTITUTIONAL-GRADE PAGINATION FOR TRADE HISTORY: Overhauled HistoryTable.jsx to restrict historical trade rendering to 10 entries per page. Implemented interactive dynamic pagination controls complete with page numeric selections (e.g. 1, 2, 3), custom ellipsis ranges for large page sets, responsive Chevron next/prev toggles, and detailed index tracker summaries.
==================================================
TODO FEATURES
--------------------------------------------------
- Binance: Add automated trade execution (via authenticated API keys) based on the generated signals.
36. SECURED ADMIN ENDPOINTS: Implemented requireRole middleware to protect backend administrative routes (settings, logs, manual scanning, telegram broadcast). Admin actions now require the { role: 'master' } verification from the frontend, checked against the backend database using the x-user-id header to prevent client-side localStorage spoofing.
37. MULTI-TIMEFRAME ATR & POSITION SIZING: Replaced single 15m ATR calculation with a weighted multi-timeframe ATR (15m=20%, 1h=50%, 4h=30%) for dynamic targets. Integrated regime-adaptive multipliers to widen Stop Loss during volatility and expand Take Profit during strong trends. Exposed Capital, Risk %, and Leverage as configurable admin settings to power dynamic dollar-based position sizing calculations.
